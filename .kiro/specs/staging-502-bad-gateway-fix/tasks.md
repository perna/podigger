# Implementation Plan

- [ ] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Docker Compose Permission Denied on .env
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For deterministic bugs, scope the property to the concrete failing case(s) to ensure reproducibility
  - Test that Docker Compose fails to read `/opt/podigger-staging/.env` when executed by user `ubuntu` with file permissions `600` and owner `deploy`
  - The test assertions should match the Expected Behavior Properties from design:
    - Assert that Docker Compose returns "permission denied" error when trying to read `.env`
    - Assert that only `db-staging` and `redis-staging` containers are running
    - Assert that `backend-staging` and `frontend-staging` containers are NOT running
    - Assert that Nginx returns 502 Bad Gateway when accessing staging URLs
  - Run test on UNFIXED code (current deploy-staging.yml with `chmod 600`)
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause:
    - Specific error message from Docker Compose
    - List of running vs. non-running containers
    - HTTP status codes from staging URLs
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Production and Security Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (production deploys, backup operations, nginx configuration)
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - Property: Production deploy uses `/opt/podigger-production/.env` with independent permissions
    - Property: Infrastructure containers (PostgreSQL, Redis) start correctly regardless of application container status
    - Property: Nginx remains in `podigger-proxy` network and has access to containers
    - Property: `.env` file has no permissions for "others" (third permission digit is 0)
    - Property: Backup process creates database backups successfully
    - Property: Rollback process restores backups correctly on failure
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3_

- [ ] 3. Fix for staging 502 Bad Gateway due to .env permission denied

  - [ ] 3.1 Update deploy-staging.yml to fix .env permissions
    - Modify the "Create .env on VPS (staging)" step in `.github/workflows/deploy-staging.yml`
    - Change `chmod 600` to `chmod 640` to allow group read access
    - Add `chown deploy:deploy /opt/podigger-staging/.env` to ensure correct ownership
    - Add verification step to log file permissions after creation
    - Add idempotent check to add `ubuntu` user to `deploy` group using `usermod -aG deploy ubuntu`
    - Add conditional logic to only add user to group if not already a member
    - _Bug_Condition: isBugCondition(deployExecution) where deployExecution.environment == 'staging' AND deployExecution.executingUser == 'ubuntu' AND deployExecution.envFile.permissions == '600' AND NOT canRead(deployExecution.executingUser, deployExecution.envFile)_
    - _Expected_Behavior: Docker Compose SHALL successfully read .env file and start all containers (backend, frontend, db, redis, celery, celery-beat) without permission errors_
    - _Preservation: Production deploys, backup/restore, nginx configuration, and security restrictions SHALL remain unchanged_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3_

  - [ ] 3.2 Update rollback step to maintain correct permissions
    - Modify the "Rollback on failure" step in `.github/workflows/deploy-staging.yml`
    - Ensure `.env` file is created with `chmod 640` and `chown deploy:deploy` in rollback scenario
    - Maintain consistency with main deploy step
    - _Requirements: 3.2_

  - [ ] 3.3 Add documentation for VPS setup requirements
    - Create or update documentation file (e.g., `docs/deployment/vps-setup.md`)
    - Document requirement: User executing Docker Compose must be in `deploy` group
    - Document manual setup step: `sudo usermod -aG deploy ubuntu` (if not automated)
    - Document verification: `groups ubuntu` should show `deploy` in the list
    - Document file permission requirements: `.env` files should be `640` with owner `deploy:deploy`
    - _Requirements: 2.1_

  - [ ] 3.4 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Docker Compose Reads .env Successfully
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied:
      - Docker Compose successfully reads `/opt/podigger-staging/.env`
      - All containers start successfully (backend, frontend, db, redis, celery, celery-beat)
      - Nginx resolves container names and establishes connections
      - Staging URLs return HTTP 200 without 502 errors
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3_

  - [ ] 3.5 Verify preservation tests still pass
    - **Property 2: Preservation** - Production and Security Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all preservation properties still hold:
      - Production deploys work independently
      - Infrastructure containers function correctly
      - Nginx configuration unchanged
      - Security restrictions maintained (no "others" permissions)
      - Backup and rollback processes work correctly
    - _Requirements: 3.1, 3.2, 3.3_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run complete test suite including bug condition and preservation tests
  - Verify all containers start successfully in staging environment
  - Verify staging URLs are accessible without 502 errors
  - Verify production environment is unaffected
  - Verify security of `.env` file is maintained (no world-readable permissions)
  - If any issues arise, document them and ask the user for guidance
